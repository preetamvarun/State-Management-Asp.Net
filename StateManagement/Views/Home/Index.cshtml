@{
    Layout = null;
}

@using System.Web.Optimization

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
    @Styles.Render("~/Content/StyleBundle")
</head>
<body>
    @{ 
        // Html.RenderPartial("_Welcome");
        // Html.Partial("_Welcome");
        // Html.RenderAction("PartialAction");
        String Str = Html.Partial("_Welcome").ToString().Replace('W', 'V');
        MvcHtmlString MHS = new MvcHtmlString(Str);
        @MHS
    }
    <h3>This is the main site</h3>
    <div> 
        @* Displaying the value of sum in the default view *@
        @{ 
            int a = 10, b = 210, sum = 0;
            sum = a + b;
            <p>The value of sum is @sum</p>
        }
    </div>
    <a href="@Url.Action("Login", "Home")">Login Page</a>
    <a href="@Url.Action("Registration", "Home")">Registration Page</a>
    <a href="@Url.Action("PurchaseProduct", "Home")">Product Page</a>
    <a href="@Url.Action("FavouriteSingerDetails","Home")">Singer(Cookies)</a>
    <a href="@Url.Action("FavouriteCricketerDetails", "Home")">Cricketer(Session)</a>
    <a href="@Url.Action("App", "Home")">Facts(Application Data)</a>
    <a href="@Url.Action("AnonymousTypes", "Home")">Employee (Anonymous Types)</a>
    <br />
    <a href="@Url.Action("Product", "Home")">Product (Model)</a>
    <br/>
   <a href="@Url.Action("Cricketer", "Home")">Cricketer(Form Collection)</a>
   <a href="@Url.Action("Person","Home")">Person(Html helpers)</a>
   <a href="@Url.Action("Animal", "Home")">Animal (Strongly Typed HTML Helpers)</a>
   @Html.ActionLink("Visit Doctor Site","Index","Home",new {area = "Doctor"}, null)
   @Html.ActionLink("Visit Staff site", "Index", "Home", new {area = "Staff"}, null)
    <br/>
   @Html.ActionLink("Display People","DisplayPeople","Home", new {area = ""}, null)
</body>
</html>
